1. to compile, use makefile command "make"
*. wxwidgets library is required for compiling
2. to start game open Checker.exe, then option->start game or option->load game to load from games saved in format provided online
3. after game started/loaded, to interface with ui for human input (when the game is set to have human player(s))
    double click on the current player color, when moves are available the double clicked box's frame becomes black,
    and possible moving positions are indicated by white frame.
4. when it's agent's move, it would attemp to use as much as time as possible (less then time limit) 
5. chess.png is used as resource for UI,no usable transparent PNG was found for checker :(( 